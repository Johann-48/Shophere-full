# Backend Environment Variables
# Copie este arquivo para .env e configure suas variáveis

# Database Configuration - Desenvolvimento Local
DB_HOST=localhost
DB_USER=seu_usuario_mysql
DB_PASSWORD=sua_senha_mysql
DB_NAME=shophere_db

# Database Configuration - Staging (PlanetScale develop branch)
DB_HOST_STAGING=aws.connect.psdb.cloud
DB_USER_STAGING=seu_usuario_planetscale_staging
DB_PASSWORD_STAGING=sua_senha_planetscale_staging
DB_NAME_STAGING=shophere-db-staging

# Server Configuration
PORT=4000
FRONTEND_URL=http://localhost:5173

# JWT Secret (use um valor seguro em produção)
JWT_SECRET=seu_jwt_secret_muito_seguro_aqui

# Environment (development, staging, production)
NODE_ENV=development

# ===========================
# Gmail SMTP - Reset de Senha
# ===========================
# Configure uma conta Gmail com 2FA e gere uma Senha de App
GMAIL_USER=seu_email@gmail.com
GMAIL_APP_PASSWORD=senha_de_app_gerada
# Nome apresentado no e-mail enviado
MAIL_FROM="Seu Nome <seu_email@gmail.com>"

# URL da página de redefinição de senha no frontend
RESET_PASSWORD_PAGE_URL=https://seu-front.com/reset-password
# Tempo de expiração do token em minutos
RESET_TOKEN_EXP_MINUTES=15

# CORS Origin principal (para produção configure a URL do seu site)
# Ex.: https://seu-site.vercel.app
# FRONTEND_URL=https://seu-site.vercel.app

# ==========================
# Armazenamento de uploads
# ==========================
# Strategy de armazenamento: "local" (padrão), "s3" ou "vercel-blob"
# STORAGE_DRIVER=local

# URL pública base utilizada para construir links absolutos quando a URL salva no banco é relativa.
# Em desenvolvimento, deixe em branco (o frontend usa proxy). Em produção, defina a URL do backend/CDN.
# Ex.: https://shophere-backend.example.com
# PUBLIC_UPLOADS_BASE_URL=

# Se usar S3/R2/Spaces (STORAGE_DRIVER=s3), configure:
# S3_BUCKET=seu-bucket
# S3_REGION=us-east-1
# S3_ENDPOINT=https://<opcional-ex:accountid>.r2.cloudflarestorage.com
# S3_FORCE_PATH_STYLE=true
# S3_ACCESS_KEY_ID=xxx
# S3_SECRET_ACCESS_KEY=xxx
# S3_DISABLE_ACL=true
# S3_ACL=public-read

# Se usar Vercel Blob (STORAGE_DRIVER=vercel-blob), configure:
# BLOB_READ_WRITE_TOKEN=seu_token_do_vercel_blob

# Para Produção (adicionar no Vercel):
# DB_HOST=aws.connect.psdb.cloud
# DB_USER=seu_usuario_planetscale_prod
# DB_PASSWORD=sua_senha_planetscale_prod
# DB_NAME=shophere-db
# NODE_ENV=production